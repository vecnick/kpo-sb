# HSE Bank Finance Module

Модуль учета финансов для банковского приложения HSE-Банк, реализованный с использованием принципов SOLID, GRASP и паттернов проектирования GoF.

## Функциональность

### Основные возможности:
- **Управление счетами** - создание, просмотр, обновление и удаление банковских счетов
- **Управление категориями** - создание категорий доходов и расходов
- **Управление операциями** - регистрация финансовых операций (доходы/расходы)
- **Автоматический расчет баланса** - баланс счета обновляется автоматически при операциях
- **Финансовая аналитика** - анализ доходов, расходов, группировка по категориям

### Дополнительные функции:
- **Команды с отменой** - поддержка undo для операций
- **Измерение производительности** - замер времени выполнения операций
- **Гибкая архитектура** - легко расширяемая система

## 🏗️ Архитектура и паттерны

### Реализованные принципы SOLID:

- **SRP (Single Responsibility)** - каждый класс имеет одну ответственность
    - `BankAccountFacade` - управление счетами
    - `OperationFacade` - управление операциями
    - `AnalyticsFacade` - аналитика и отчетность

- **OCP (Open-Closed)** - система открыта для расширения
    - `DataImporter` - шаблонный метод для импорта данных
    - `DataVisitor` - посетитель для экспорта данных

- **LSP (Liskov Substitution)** - наследники заменяют родителей
    - `Repository<T>` и его реализации

- **ISP (Interface Segregation)** - разделенные интерфейсы
    - `Command` - только execute/undo
    - `Repository` - только CRUD операции

- **DIP (Dependency Inversion)** - зависимости от абстракций
    - Внедрение зависимостей через `AppConfig`

### Принципы GRASP:

- **High Cohesion** - логически связанный код сгруппирован
- **Low Coupling** - минимальные зависимости между модулями


### Порождающие:
1. **Фабрика (Factory)** - `DomainFactory` - централизованное создание доменных объектов с валидацией
2. **Строитель (Builder)** - неявно в фабричных методах

### Структурные:
3. **Фасад (Facade)** - `BankAccountFacade`, `OperationFacade`, `CategoryFacade`, `AnalyticsFacade` - упрощенные интерфейсы для сложных подсистем
4. **Декоратор (Decorator)** - `TimingDecorator` - добавление измерения времени к командам
5. **Прокси (Proxy)** - `RepositoryProxy` - кэширование данных (заготовка)

### Поведенческие:
6. **Команда (Command)** - `CreateBankAccountCommand`, `CreateOperationCommand` - инкапсуляция запросов
7. **Шаблонный метод (Template Method)** - `DataImporter` - общий алгоритм импорта
8. **Посетитель (Visitor)** - `DataVisitor` - обработка различных типов данных для экспорта

##  Структура проекта
жение демонстрирует создание счетов, операции и аналитику с использованием паттернов.